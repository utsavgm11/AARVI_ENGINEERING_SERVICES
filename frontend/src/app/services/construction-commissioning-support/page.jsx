// ─── NOTE FOR NEXT.JS 13+ APP ROUTER ──────────────────────────────────────────
// "use client" pages can't export `metadata`. Put this in a sibling
// page.server.js / layout.js / metadata.js file:
//
// export const metadata = {
//   title: "Construction, Commissioning & Asset Support | Aarvi Engineering Services",
//   description: "Expert site engineering, mechanical completion, pre-commissioning, start-up support, and asset reliability engineering.",
//   keywords: ["construction support","commissioning","mechanical completion","start-up support","turnaround support","reliability engineering","SAP PM","IBM Maximo"],
//   openGraph: { title: "Construction, Commissioning & Asset Support", type: "website" }
// };
// ──────────────────────────────────────────────────────────────────────────────

"use client";
import React, { useState, useRef } from "react";
import { motion, AnimatePresence } from "framer-motion";
import Link from "next/link";
import Image from "next/image";
import Head from "next/head";

import {
  HardHat, ClipboardCheck, Power, Activity, SearchCheck, 
  RotateCcw, PenTool, DatabaseBackup, ArrowUpRight, ShieldCheck,
  Users, Clock, BadgeCheck, Briefcase
} from "lucide-react";

// ─── ASSET IMPORTS ───────────────────────────────────────────────────────────

// Use a single, unified image for all capabilities as requested
// Uncomment the line below when your image is in the assets folder
import sharedCapabilityImg from '../../../assets/cc_support.png';

// Fallback to prevent Next.js from crashing while the import is commented out


// ─── CAPABILITIES DATA ───────────────────────────────────────────────────────
const CAPABILITIES = [
  {
    num: "01", code: "CC-101", icon: HardHat,
    image: sharedCapabilityImg,
    title: "Site Engineering & Construction Support",
    shortDesc: "Field-level technical resolution to ensure construction perfectly matches design intent.",
    fullDesc: "Deploying senior field engineers to live construction sites to resolve unforeseen technical queries (TQs) and issue Field Change Requests (FCRs). We ensure contractors strictly adhere to 'Approved for Construction' (AFC) drawings, mitigating schedule delays caused by engineering bottlenecks.",
    deliverables: ["Site Technical Query (STQ) Resolutions", "Field Change Requests (FCR)", "Constructability Review Audits", "Redline Markup Approvals"],
    keyOutcomes: ["Uninterrupted construction", "Preserved design integrity", "Rapid problem solving", "Zero field rework"],
    applications: ["Greenfield Construction", "Brownfield Erection", "Skid Integration", "Module Fabrication Yards"]
  },
  {
    num: "02", code: "CC-102", icon: ClipboardCheck,
    image: sharedCapabilityImg,
    title: "Mechanical Completion Management",
    shortDesc: "Rigorous system walkdowns and punch-list management prior to energization.",
    fullDesc: "Managing the critical transition from construction to pre-commissioning. We define system boundaries, conduct rigorous field walkdowns, and aggressively manage Category A and B punch-lists to achieve certified Mechanical Completion (MC) without compromising safety.",
    deliverables: ["Mechanical Completion Certificates", "Punch-list Registers", "System Boundary Matrices", "Inspection Test Records (ITR)"],
    keyOutcomes: ["Safe energization readiness", "Clear contractor accountability", "Systematic quality control", "Schedule milestone achievement"],
    applications: ["EPC Handover Phases", "Package Unit Sign-offs", "Piping Hydrotest Clearances", "Electrical Loop Verifications"]
  },
  {
    num: "03", code: "CC-103", icon: Power,
    image: sharedCapabilityImg,
    title: "Pre-Commissioning & Start-Up Support",
    shortDesc: "Methodical system flushing, loop checking, and functional testing sequences.",
    fullDesc: "Executing the detailed pre-commissioning sequences required to bring a dormant plant to life. We oversee pipe flushing, chemical cleaning, instrument loop checks, and motor solo runs, ensuring every component functions perfectly before the introduction of live hydrocarbons or power.",
    deliverables: ["Pre-Commissioning Procedures", "Flushing & Blowing Plans", "Loop Check Dossiers", "Start-up Sequencing Manuals"],
    keyOutcomes: ["Flawless system integration", "Eliminated startup hazards", "Validated instrumentation", "Clean process piping"],
    applications: ["Process Train Start-ups", "Utility System Energization", "Compressor First-Runs", "DCS Logic Validations"]
  },
  {
    num: "04", code: "CC-104", icon: Activity,
    image: sharedCapabilityImg,
    title: "Commissioning & Performance Testing",
    shortDesc: "Live system tuning, capacity proving, and OEM performance guarantee validation.",
    fullDesc: "Leading the live commissioning phase and final performance test runs (PTR). We introduce live process fluids, tune complex control loops, and push the facility to its nameplate capacity to validate licensor and OEM performance guarantees before final client handover.",
    deliverables: ["Performance Test Run (PTR) Reports", "System Tuning Logs", "Capacity Proving Certificates", "Final Handover Dossiers"],
    keyOutcomes: ["Validated ROI", "Licensor guarantee confirmation", "Optimized process stability", "Commercial risk mitigation"],
    applications: ["Plant Nameplate Proving", "OEM Equipment Acceptance", "Energy Efficiency Validation", "Emissions Compliance Checks"]
  },
  {
    num: "05", code: "CC-105", icon: SearchCheck,
    image: sharedCapabilityImg,
    title: "Vendor Inspection & Expediting",
    shortDesc: "Global supply chain oversight ensuring equipment quality and delivery timelines.",
    fullDesc: "Deploying specialized inspectors to global manufacturing facilities to conduct Factory Acceptance Tests (FAT) and Site Integration Tests (SIT). We aggressively expedite critical path materials and ensure fabrication strictly aligns with project specifications.",
    deliverables: ["FAT/SIT Inspection Reports", "Material Expediting Dashboards", "Non-Conformance Reports (NCR)", "Release for Shipment (RFS) Certificates"],
    keyOutcomes: ["Quality assured fabrication", "Prevented delivery delays", "Code-compliant materials", "Eliminated site fit-up issues"],
    applications: ["Critical Rotating Machinery", "Heavy Pressure Vessels", "Complex Control Panels", "Long Lead Bulk Materials"]
  },
  {
    num: "06", code: "CC-106", icon: RotateCcw,
    image: sharedCapabilityImg,
    title: "Shutdown & Turnaround Support",
    shortDesc: "High-intensity engineering support for time-critical plant maintenance windows.",
    fullDesc: "Providing rapid-response engineering and planning support during high-pressure plant turnarounds (TAR). We develop detailed execution schedules, coordinate massive concurrent contractor activities, and engineer immediate solutions for unexpected corrosion or metallurgical findings.",
    deliverables: ["Turnaround Execution Schedules", "Critical Path Recovery Plans", "Emergent Work Engineering Solutions", "TAR Close-out Reports"],
    keyOutcomes: ["Minimized production downtime", "Safe concurrent operations", "Budget containment", "Rapid crisis resolution"],
    applications: ["Scheduled Plant Outages", "Emergency Shutdowns (ESD)", "Catalyst Replacements", "Major Equipment Overhauls"]
  },
  {
    num: "07", code: "CC-107", icon: PenTool,
    image: sharedCapabilityImg,
    title: "Maintenance & Reliability Engineering",
    shortDesc: "Data-driven strategies to maximize asset uptime and prevent catastrophic failures.",
    fullDesc: "Developing proactive reliability frameworks including Reliability Centered Maintenance (RCM) and Failure Mode and Effects Analysis (FMEA). We optimize spare parts inventory, define predictive maintenance routines, and configure CMMS platforms (SAP PM/Maximo) to extend asset lifecycles.",
    deliverables: ["RCM / FMEA Studies", "Preventative Maintenance (PM) Plans", "Critical Spare Parts Lists", "CMMS Data Loading Templates"],
    keyOutcomes: ["Maximized asset uptime", "Reduced OPEX", "Data-driven maintenance", "Prevented unplanned outages"],
    applications: ["Aging Infrastructure", "Rotating Equipment Fleets", "SAP/Maximo Implementations", "Reliability Audits"]
  },
  {
    num: "08", code: "CC-108", icon: DatabaseBackup,
    image: sharedCapabilityImg,
    title: "Asset Integrity & Handover Documentation",
    shortDesc: "Compiling certified operational dossiers to secure regulatory and insurance compliance.",
    fullDesc: "Structuring the final digital and physical handover of the facility. We compile perfectly reconciled as-built drawings, vendor databooks, and operational manuals into intelligent databases (SmartPlant/AVEVA), ensuring operators have instant access to critical safety data.",
    deliverables: ["Final As-Built Dossiers", "Vendor Data Books (VDB)", "Intelligent Linked PDFs", "Asset Information Management (AIM) Setup"],
    keyOutcomes: ["Seamless operational transition", "Regulatory audit readiness", "Instant data retrieval", "Contractual closure"],
    applications: ["Project Close-outs", "EPC Handover", "Digital Twin Foundations", "Operations Portals"]
  }
];

const SOFTWARE_TOOLS = [
  { name: "SAP PM", category: "Maintenance Management" },
  { name: "IBM Maximo", category: "Asset Management" },
  { name: "Primavera P6", category: "Turnaround Scheduling" },
  { name: "MS Project", category: "Project Planning" },
  { name: "SmartPlant Foundation", category: "Information Management" },
  { name: "AVEVA AIM", category: "Asset Information Hub" }
];

const FEATURED_PROJECT = null; 

// ─── ANIMATION VARIANTS ───────────────────────────────────────────────────────
const fadeUp = {
  hidden: { opacity: 0, y: 24 },
  show: { opacity: 1, y: 0, transition: { duration: 0.5, ease: "easeOut" } }
};
const stagger = { hidden: {}, show: { transition: { staggerChildren: 0.07 } } };

// ─── COMPONENT ────────────────────────────────────────────────────────────────
export default function ConstructionCommissioningPage() {
  const [activeCap, setActiveCap] = useState(0);
  const [isMarqueeHovered, setIsMarqueeHovered] = useState(false);
  const [isMarqueeClicked, setIsMarqueeClicked] = useState(false);
  const detailsRef = useRef(null);
  
  const active = CAPABILITIES[activeCap];

  const carouselRef = useRef(null);
  const [isDragging, setIsDragging] = useState(false);
  const [startX, setStartX] = useState(0);
  const [scrollLeft, setScrollLeft] = useState(0);

  // Mouse Drag Handlers for Desktop
  const handleMouseDown = (e) => {
    setIsDragging(true);
    setStartX(e.pageX - carouselRef.current.offsetLeft);
    setScrollLeft(carouselRef.current.scrollLeft);
  };

  const handleMouseLeave = () => {
    setIsDragging(false);
    setIsMarqueeHovered(false); // Resume auto-scroll when mouse leaves
  };

  const handleMouseUp = () => {
    setIsDragging(false);
  };

  const handleMouseMove = (e) => {
    if (!isDragging) return;
    e.preventDefault();
    const x = e.pageX - carouselRef.current.offsetLeft;
    const walk = (x - startX) * 1.5; // 1.5 is the scroll speed multiplier
    carouselRef.current.scrollLeft = scrollLeft - walk;
  };

  const handleSelectCapability = (idx) => {
    setActiveCap(idx);
    setIsMarqueeClicked(true); // Permanently pauses the marquee so user can read below

    if (detailsRef.current) {
      window.scrollTo({
        top: detailsRef.current.getBoundingClientRect().top + window.scrollY - 96,
        behavior: "smooth",
      });
    }
  };

  return (
    <>
      <Head>
        <title>Construction, Commissioning & Asset Support | Aarvi Engineering</title>
        <meta
          name="description"
          content="Expert site engineering, mechanical completion, pre-commissioning, start-up support, and asset reliability engineering."
        />
        <meta name="keywords" content="construction support, commissioning, mechanical completion, start-up support, turnaround support, reliability engineering, SAP PM, IBM Maximo" />
        <meta property="og:title" content="Construction, Commissioning & Asset Support | Aarvi Engineering" />
        <meta property="og:description" content="Bridging the gap between engineering design and flawless facility operations." />
        <meta property="og:type" content="website" />
      </Head>

      <main className="w-full bg-aarvi-bg min-h-screen text-aarvi-navy font-sans selection:bg-aarvi-green/20 selection:text-aarvi-navy">

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 1 · HERO — video bg + left text, exactly per reference layout      */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section className="relative w-full min-h-[78vh] flex items-center overflow-hidden bg-aarvi-navy">
          <div className="absolute inset-0 w-full h-full">
            {/* ─── ADD BG VIDEO HERE (Uncomment and replace src when ready) ─── */}
            { <video
              autoPlay muted loop playsInline
              className="absolute inset-0 w-full h-full object-cover"
              aria-hidden="true"
            >
              <source src="/commissioning-hero.mp4" type="video/mp4" />
            </video>
            }
            <div className="absolute inset-0 bg-linear-to-r from-aarvi-navy via-aarvi-navy/85 to-aarvi-navy/30" />
            <div className="absolute inset-0 bg-aarvi-navy/20" />
          </div>

          <div className="relative z-10 max-w-7xl mx-auto px-6 lg:px-10 py-10 lg:py-14 w-full">
            <motion.div
              initial="hidden"
              animate="show"
              variants={stagger}
              className="max-w-2xl"
            >
              <motion.div variants={fadeUp} className="flex items-center gap-2 mb-5">
                <span className="w-1.5 h-1.5 rounded-full bg-aarvi-green animate-pulse" />
                <span className="text-[11px] font-mono font-bold text-aarvi-green tracking-[0.25em] uppercase">
                  Field Execution & Handover
                </span>
              </motion.div>

              <motion.h1
                variants={fadeUp}
                className="text-4xl md:text-5xl lg:text-6xl font-black text-white uppercase tracking-tight leading-[1.05] mb-5"
              >
                Construction, Commissioning<br />& Asset Support
              </motion.h1>

              <motion.div variants={fadeUp} className="flex flex-wrap items-center gap-x-3 gap-y-1 text-sm font-bold text-white/60 mb-6">
                {["Site Engineering", "Mechanical Completion", "Start-Up Support", "Turnarounds", "Reliability"].map((t, i) => (
                  <React.Fragment key={t}>
                    <span>{t}</span>
                    {i < 4 && <span className="text-white/25">•</span>}
                  </React.Fragment>
                ))}
              </motion.div>

              <motion.p variants={fadeUp} className="text-base lg:text-lg text-white/70 leading-relaxed max-w-lg mb-9">
                Bridging the critical gap between detailed design and live operations. We provide high-intensity field engineering, flawless start-up sequences, and data-driven reliability frameworks.
              </motion.p>

              <motion.div variants={fadeUp} className="grid grid-cols-3 gap-6 mb-9 pb-9 border-b border-white/15 max-w-lg">
                {[
                  { icon: HardHat, value: "100%", label: "Safe Execution" },
                  { icon: ShieldCheck, value: "Sync", label: "Design Validation" },
                  { icon: BadgeCheck, value: "Zero", label: "Startup Delays" }
                ].map(({ icon: Icon, value, label }) => (
                  <div key={label}>
                    <div className="flex items-center gap-1.5 mb-1">
                      <Icon className="w-4 h-4 text-aarvi-green" strokeWidth={1.5} />
                      <span className="text-2xl lg:text-3xl font-black text-white leading-none">{value}</span>
                    </div>
                    <span className="text-[10px] text-white/50 font-mono uppercase tracking-widest">{label}</span>
                  </div>
                ))}
              </motion.div>

            </motion.div>
          </div>
        </section>

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 2 · CORE CAPABILITIES — Infinite Horizontal Marquee                */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section className="bg-white py-10 lg:py-14 border-b border-slate-200 overflow-hidden">
          <div className="max-w-7xl mx-auto px-6 lg:px-10">
            <motion.div 
              initial="hidden" 
              whileInView="show" 
              viewport={{ once: true, margin: "-80px" }} 
              variants={stagger} 
              className="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-10"
            >
              <div>
                <motion.h2 
                  variants={fadeUp} 
                  className="text-3xl md:text-4xl font-black text-aarvi-navy uppercase tracking-tight"
                >
                  Integrated Field Expertise
                </motion.h2>
              </div>
            </motion.div>
          </div>

          {/* Interactive Scrolling Track */}
          <div 
            ref={carouselRef}
            className={`w-full flex overflow-x-auto pb-6 select-none scrollbar-none [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden ${
              isDragging ? "cursor-grabbing" : "cursor-grab"
            }`}
            onMouseEnter={() => setIsMarqueeHovered(true)}
            onMouseLeave={handleMouseLeave}
            onMouseDown={handleMouseDown}
            onMouseUp={handleMouseUp}
            onMouseMove={handleMouseMove}
            onTouchStart={() => setIsMarqueeHovered(true)} 
            onTouchEnd={() => setIsMarqueeHovered(false)}
          >
            <div 
              className={`flex gap-4 px-4 whitespace-nowrap will-change-transform animate-marquee ${
                isMarqueeHovered || isMarqueeClicked || isDragging ? '[animation-play-state:paused]' : ''
              }`}
            >
              {[...CAPABILITIES, ...CAPABILITIES].map((cap, idx) => {
                const originalIndex = idx % CAPABILITIES.length;
                const isActive = originalIndex === activeCap;
                const Icon = cap.icon;
                
                return (
                  <motion.button 
                    key={`${cap.code}-${idx}`} 
                    initial={{ opacity: 0, y: 16 }} 
                    whileInView={{ opacity: 1, y: 0 }} 
                    viewport={{ once: true, margin: "-60px" }} 
                    transition={{ delay: originalIndex * 0.05, duration: 0.4 }} 
                    onClick={() => handleSelectCapability(originalIndex)} 
                    className={`group relative shrink-0 w-70 sm:w-[320px] p-5 rounded-2xl border-2 text-left transition-all duration-300 pointer-events-auto ${
                      isActive 
                        ? "bg-white border-aarvi-green shadow-lg shadow-aarvi-green/10 -translate-y-1" 
                        : "bg-white border-slate-200 hover:border-aarvi-green/50 hover:shadow-md hover:-translate-y-1"
                    }`}
                  >
                    <div className="flex items-start justify-between mb-4">
                      <div className={`w-10 h-10 rounded-lg border flex items-center justify-center transition-all duration-300 ${
                        isActive 
                          ? "bg-aarvi-green border-aarvi-green text-white" 
                          : "bg-aarvi-bg border-slate-200 text-slate-400 group-hover:text-aarvi-green group-hover:border-aarvi-green/40"
                      }`}
                      >
                        <Icon className="w-4.5 h-4.5" />
                      </div>
                    </div>

                    <h3 className="text-sm font-black text-aarvi-navy uppercase leading-tight tracking-tight mb-2 min-h-[2.2em] whitespace-normal">
                      {cap.title}
                    </h3>
                    
                    <p className="text-[11px] text-slate-400 leading-relaxed line-clamp-2 whitespace-normal">
                      {cap.shortDesc}
                    </p>

                    {isActive && (
                      <div className="mt-3 pt-3 border-t border-aarvi-green/15 flex items-center gap-1">
                        <span className="text-[9px] font-mono font-black text-aarvi-green uppercase tracking-widest">
                          Selected
                        </span>
                      </div>
                    )}
                  </motion.button>
                );
              })}
            </div>
          </div>
        </section>

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 3 · CAPABILITY DETAIL — 70% content / 30% wireframe, equal height */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section ref={detailsRef} id="service-details" className="bg-aarvi-bg py-10 lg:py-14 border-b border-slate-200 scroll-mt-24">
          <div className="max-w-7xl mx-auto px-6 lg:px-10">
            <AnimatePresence mode="wait">
              <motion.div
                key={activeCap}
                initial={{ opacity: 0, y: 16 }}
                animate={{ opacity: 1, y: 0 }}
                exit={{ opacity: 0, y: -8 }}
                transition={{ duration: 0.35 }}
                className="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden flex flex-col lg:flex-row"
              >
                {/* ─ 70%: Capability content ─ */}
                <div className="w-full lg:w-[70%] p-8 lg:p-12">
                  <div className="flex items-start gap-4 mb-7">
                    <div>
                      <h2 className="text-2xl lg:text-3xl font-black text-aarvi-navy uppercase tracking-tight leading-tight">
                        {active.title}
                      </h2>
                    </div>
                  </div>

                  <p className="text-text-body leading-relaxed mb-9 text-[15px] max-w-2xl">
                    {active.fullDesc}
                  </p>

                  <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div>
                      <p className="text-[10px] font-mono font-black text-slate-400 uppercase tracking-widest mb-3">
                        Deliverables
                      </p>
                      <ul className="space-y-2.5">
                        {active.deliverables.map((item) => (
                          <li key={item} className="flex items-start gap-2 text-sm text-text-body">
                            <span className="text-aarvi-green font-bold mt-0.5 shrink-0">✓</span>
                            {item}
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div>
                      <p className="text-[10px] font-mono font-black text-slate-400 uppercase tracking-widest mb-3">
                        Key Outcomes
                      </p>
                      <ul className="space-y-2.5">
                        {active.keyOutcomes.map((item) => (
                          <li key={item} className="flex items-start gap-2 text-sm text-text-body">
                            <span className="text-aarvi-green font-bold mt-0.5 shrink-0">✓</span>
                            {item}
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div>
                      <p className="text-[10px] font-mono font-black text-slate-400 uppercase tracking-widest mb-3">
                        Typical Applications
                      </p>
                      <ul className="space-y-2.5">
                        {active.applications.map((item) => (
                          <li key={item} className="flex items-start gap-2 text-sm text-text-body">
                            <span className="text-slate-300 font-bold mt-0.5 shrink-0">•</span>
                            {item}
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>

                {/* ─ 30%: Wireframe visual panel ─ */}
                <div className="w-full lg:w-[30%] relative min-h-75 lg:min-h-0 bg-linear-to-br from-aarvi-navy to-[#16213d] border-t lg:border-t-0 lg:border-l border-slate-200">
  {active.image && (
    <Image
      src={active.image}
      alt={`${active.title} — technical schematic`}
      fill
      sizes="(max-width: 1024px) 100vw, 30vw"
      className="object-cover opacity-90 z-10 mix-blend-screen"
    />
  )}

  <div
    className="absolute inset-0 opacity-[0.08]"
    style={{
      backgroundImage:
        "linear-gradient(rgba(255,255,255,1) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,1) 1px,transparent 1px)",
      backgroundSize: "32px 32px",
    }}
  />
</div>
              </motion.div>
            </AnimatePresence>
          </div>
        </section>

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 4 · SOFTWARE ECOSYSTEM                                             */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section className="bg-white py-10 lg:py-14 border-b border-slate-200">
          <div className="max-w-7xl mx-auto px-6 lg:px-10">
            <motion.div
              initial="hidden"
              whileInView="show"
              viewport={{ once: true, margin: "-80px" }}
              variants={stagger}
              className="text-center mb-12"
            >
              <motion.div variants={fadeUp} className="flex items-center justify-center gap-2 mb-3">
                <ShieldCheck className="w-5 h-5 text-aarvi-green" strokeWidth={1.5} />
                <span className="text-[10px] font-mono font-black text-aarvi-green tracking-[0.3em] uppercase">
                  Software Ecosystem
                </span>
              </motion.div>
              <motion.h2 variants={fadeUp} className="text-3xl md:text-4xl font-black text-aarvi-navy uppercase tracking-tight mb-3">
                Advanced Tools. Proven Results.
              </motion.h2>
              <motion.p variants={fadeUp} className="text-text-body text-sm font-medium max-w-xl mx-auto">
                We leverage the world&apos;s most advanced planning, maintenance, and asset management
                environments to ensure absolute execution precision.
              </motion.p>
            </motion.div>

            <motion.div
              initial="hidden"
              whileInView="show"
              viewport={{ once: true, margin: "-80px" }}
              variants={stagger}
              className="flex flex-wrap justify-center gap-4"
            >
              {SOFTWARE_TOOLS.map((tool) => (
                <motion.div
                  key={tool.name}
                  variants={fadeUp}
                  className="p-5 w-48 rounded-xl border border-slate-200 bg-aarvi-bg hover:border-aarvi-green/40 hover:bg-white hover:shadow-md hover:-translate-y-1 transition-all duration-300 flex flex-col items-center justify-center text-center cursor-default min-h-24"
                >
                  <div className="font-mono text-xs font-black text-aarvi-navy uppercase tracking-widest leading-tight mb-1.5">
                    {tool.name}
                  </div>
                  <div className="text-[9px] text-slate-400 font-bold uppercase tracking-widest">{tool.category}</div>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </section>

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 6 · FEATURED PROJECT — wired for DB later                        */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section className="bg-white py-10 lg:py-14 border-b border-slate-200">
          <div className="max-w-7xl mx-auto px-6 lg:px-10">
            <motion.span
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              viewport={{ once: true }}
              className="text-[10px] font-mono font-black text-aarvi-green tracking-[0.3em] uppercase block mb-8"
            >
              Featured Case Study
            </motion.span>

            {FEATURED_PROJECT ? (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch"
              >
                <div className="lg:col-span-5 relative rounded-2xl overflow-hidden min-h-75 bg-linear-to-br from-aarvi-navy to-[#16213d]" />

                <div className="lg:col-span-7 flex flex-col justify-center">
                  <h3 className="text-2xl font-black text-aarvi-navy uppercase tracking-tight mb-4">
                    {FEATURED_PROJECT.title}
                  </h3>
                  <p className="text-text-body text-sm leading-relaxed mb-6">{FEATURED_PROJECT.scope}</p>
                  <Link
                    href="/projects"
                    className="inline-flex items-center gap-2 text-aarvi-navy font-black text-xs uppercase tracking-widest hover:text-aarvi-green transition-colors w-fit"
                  >
                    View Case Study <ArrowUpRight className="w-3.5 h-3.5" />
                  </Link>
                </div>
              </motion.div>
            ) : (
              <motion.div
                initial={{ opacity: 0, y: 16 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                className="rounded-2xl border-2 border-dashed border-slate-200 bg-aarvi-bg p-12 lg:p-16 flex flex-col items-center text-center"
              >
                <div className="w-14 h-14 rounded-xl bg-white border border-slate-200 flex items-center justify-center mb-5">
                  <Briefcase className="w-6 h-6 text-slate-300" strokeWidth={1.5} />
                </div>
                <h3 className="text-lg font-black text-aarvi-navy uppercase tracking-tight mb-2">
                  Project Case Studies Coming Soon
                </h3>
                <p className="text-text-body text-sm max-w-md mb-6">
                  This section will automatically pull a relevant featured project for
                  Construction & Commissioning from the projects database once connected.
                </p>
                <Link
                  href="/projects"
                  className="inline-flex items-center gap-2 px-5 py-2.5 border border-slate-200 text-aarvi-navy font-black text-xs uppercase tracking-widest rounded-lg hover:border-aarvi-green hover:text-aarvi-green transition-colors"
                >
                  Browse All Projects <ArrowUpRight className="w-3.5 h-3.5" />
                </Link>
              </motion.div>
            )}
          </div>
        </section>

        {/* ══════════════════════════════════════════════════════════════════ */}
        {/* 7 · CTA BANNER                                                     */}
        {/* ══════════════════════════════════════════════════════════════════ */}
        <section className="relative bg-aarvi-navy py-10 lg:py-14 overflow-hidden">
          <div
            className="absolute inset-0 pointer-events-none opacity-[0.05]"
            style={{
              backgroundImage: "radial-gradient(circle, #00875A 1px, transparent 1px)",
              backgroundSize: "32px 32px"
            }}
          />
          <div className="relative z-10 max-w-7xl mx-auto px-6 lg:px-10">
            <motion.div
              initial="hidden"
              whileInView="show"
              viewport={{ once: true }}
              variants={stagger}
              className="flex flex-col md:flex-row items-start md:items-center justify-between gap-10"
            >
              <div className="max-w-lg">
                <motion.span variants={fadeUp} className="text-aarvi-green text-[11px] font-bold uppercase tracking-[0.25em] mb-4 block">
                  ┼ Let&apos;s Start Up Safely
                </motion.span>
                <motion.h2 variants={fadeUp} className="text-3xl md:text-4xl font-black text-white uppercase tracking-tight leading-tight mb-4">
                  Need Field Execution Expertise?
                </motion.h2>
                <motion.p variants={fadeUp} className="text-white/60 text-sm leading-relaxed">
                  Our site engineers are ready to drive your mechanical completion, commissioning, and operational reliability goals.
                </motion.p>
              </div>

              <motion.div variants={fadeUp} className="flex flex-col sm:flex-row gap-3 shrink-0">
                <Link
                  href="/contact?service=construction-commissioning-support"
                  className="inline-flex items-center justify-center gap-2 px-7 py-4 bg-aarvi-green text-white font-black text-sm uppercase tracking-widest rounded-lg hover:bg-[#00744d] shadow-[0_8px_20px_rgba(0,135,90,0.3)] hover:shadow-[0_12px_28px_rgba(0,135,90,0.4)] transition-all group"
                >
                  Talk to Our Experts
                  <ArrowUpRight className="w-4 h-4 group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform" />
                </Link>
              </motion.div>
            </motion.div>
          </div>
        </section>

      </main>
    </>
  );
}